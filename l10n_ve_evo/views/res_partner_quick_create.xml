<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_partner_quick_create_l10n_ve" model="ir.ui.view">
        <field name="name">res.partner.quick.create.l10n.ve</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_simple_form"/>

        <field name="arch" type="xml">

            <!-- ✅ Insertar rif_type después del nombre -->
            <xpath expr="//field[@name='name']" position="after">
                <field name="rif_type"/>
            </xpath>

        </field>
    </record>

</odoo>
